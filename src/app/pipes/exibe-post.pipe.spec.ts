import { ExibePostPipe } from './exibe-post.pipe';

describe('ExibePostPipe', () => {
  it('create an instance', () => {
    const pipe = new ExibePostPipe();
    expect(pipe).toBeTruthy();
  });
});
